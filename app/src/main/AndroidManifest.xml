<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthconnectai">

    <!-- ✅ Permisos necesarios -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthConnectAI">

        <!-- 🗺️ Clave de Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAiq6H_qiNeVXv1PAW8aTo-RobHWc2Ks4Y" />

        <!-- 🧩 Activities secundarias -->
        <activity android:name=".ui.tasks.TareasActivity" />
        <activity android:name=".AlertsActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".ImageAnalysisActivity" />
        <activity android:name=".AudioAnalysisActivity" />

        <!-- 🏠 Pantalla principal -->
        <activity android:name=".MainActivity" />

        <!-- ✨ NUEVA PANTALLA DE PRESENTACIÓN (Splash) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.HealthConnectAI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
